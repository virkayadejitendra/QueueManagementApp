export const environment = {
  production: true,
  // Use a relative URL in production when Angular and the API are hosted together.
  // Change this to your deployed API origin if they are hosted separately.
  apiBaseUrl: ''
};
